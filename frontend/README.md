# Isharati website

Next.js App Router, Arabic RTL. The typed FastAPI client lives in `src/lib/api.ts`.

```bash
npm install
npm run dev
```

Dev API: `http://127.0.0.1:8000` (`NEXT_PUBLIC_API_URL`). CORS allows `http://localhost:3000`.
