# Translate

Free-form two-way translator, not a lesson.

- Arabic text or speech → sign video (`textToSign` / `speechToSign` + `videoUrl`)
- Sign video → Arabic sentence (`signToText` or `fullSentenceVideo`)
